package testData.collision;

public class Main {
    int boi = 0;
    public static void main(String[] arg<caret>s, int collision1) {
        boi = args.length;
        if (args.length > 0)
            String collision2 = args[0]
    }
}
